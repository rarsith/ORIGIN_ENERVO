# Origin(DbPath.to_asset, DbAssetAttributes.is_active).get
# Origin(DbPath.to_asset, DbAssetAttributes.tasks).get
# Origin(DbPath.to_asset, DbAssetAttributes.sync_tasks).get
# Origin(DbPath.to_asset, DbAssetAttributes.assignments).get
# Origin(DbPath.to_asset, DbAssetAttributes.master_bundle).get


# Origin(DbPath.to_asset, DbAssetAttributes.curr_task.status).set(DbStatuses.pending_review)
# Origin(DbPath.to_asset, DbAssetAttributes.curr_task.imports_from=list()).set
# Origin(DbPath.to_pubslot, DbAttributes.type).get

'''

get
update
add
set
clear
reference
dereference

'''

